using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SnapPointScript : MonoBehaviour
{
	//RONG snapping isnt the only way bridges can be connected!
	/*
	private GameObject SnappedToPoint;

    // Start is called before the first frame update
    public GameObject GetSnapped()
    {
        return SnappedToPoint;
    }

    public void SetSnapped(GameObject NewSnap)
    {
    	SnappedToPoint = NewSnap;
    }
    */
}
